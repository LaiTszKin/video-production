interface:
  display_name: "Video Production"
  short_description: "Create narrated subtitle videos (single or multi-clip) from text and storyboard images"
  default_prompt: "Use $video-production to convert text into storyboard images, generate full voice + subtitles, and render one long video or multiple short clips. Ask for missing orientation, delivery mode (single/multi), and required duration values before rendering."
